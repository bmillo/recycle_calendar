'use strict';

describe('API', function() {
    it('should return Hello JSON', function() {
        debugger;
        //expect(scope.users.length).toBe(2);
        //expect(scope.users[0].name).toBe('Bob');
    });
});